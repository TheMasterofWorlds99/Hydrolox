extern func i32 printf(fmt: str, ...); // Takes a fat pointer, but 
                                       // unwraps it to C during the code Generation

func i32 start() {
    x: i32 = 10;
    msg: str = "Score: %d\n"; 
    
    printf(msg, x);
    return 0;
}
